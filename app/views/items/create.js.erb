<% if @item.valid? %>
$('.items').prepend("<%= escape_javascript(render(@item)) %>");
 <% else %>
$('.item-form').html("<%= escape_javascript(render partial: 'items/form', locals: { list: @list, item: @item }) %>");
 <% end %>